dependencies:
    pre:
        - nvm install 0.10
        - nvm install 0.12
        - nvm install 4
        - nvm install 6
test:
    override:
        - nvm use 0.10 && npm test
        - nvm use 0.12 && npm test
        - nvm use 4 && npm test
        - nvm use 6 && npm test
