dependencies:
    pre:
        - nvm install 6
        - nvm install 8
        - nvm install 10
test:
    override:
        - nvm use 6 && npm test
        - nvm use 8 && npm test
        - nvm use 10 && npm test
